<h1>Activities#index</h1>

<% @activities.each do |activity| %>
  <div class="activity">
    <%= link_to activity.owner.try(:email), profiles_path(activity.owner_id) %>
    <%= render_activity activity %>
  </div>
<% end %>

<%#= link_to activity.owner.try(:email), redirect_to action: "show", id: activity.owner_id %>
