

<div class="wrapper">
  <h1>
    Oh, hey there
  </h1>
  <h2>
    Go ahead and click on the blocks to watch 'em pop
  </h2>
  <% (1..2).each do |i| %>
    <input id="<%= i %>" type="checkbox"></input>
    <label for="<%= i %>">
      <div>
        <div class="circle"></div>
        <span>Click me</span>
      </div>
      <p>Congrats, you managed to click the box! Thanks for checking out this pen. Take a look at some of my other stuff too.</p>
      <a href="http://codepen.io/jcoulterdesign/">View my other stuff</a>
    </label>
  <% end %>
</div>






<p id="notice"><%= notice %></p>

<h1>My Profile</h1>

<table>
  <thead>
    <!-- <tr>
      <th>User</th>
      <th>Name</th>
      <th>Picture</th>
      <th>Bio</th>
      <th colspan="3"></th>
    </tr> -->
  </thead>

  <tbody>

        <% if current_user.profile && current_user.profile.vendor%>
        <td><%= current_user.profile.name %></td>
        <td><%= image_tag current_user.profile.picture,
                          id: "HomeImage",
                          class: "profilepic" %></td>
        <td><%= current_user.profile.bio %></td>

        <h2 class = "profilevendor">
          <strong>Company:</strong>
          <%= current_user.profile.company %>
        </h2>

        <h2 class = "profilevendor">
          <strong>Address:</strong>
          <%= current_user.profile.address %>
        </h2>
        <h2 class = "profilevendor">
          <strong>Phone:</strong>
          <%= current_user.profile.phone %>
        </h2>
        <h2 class = "profilevendor">
          <strong>License Type:</strong>
          <%= current_user.profile.license_type %>
        </h2>
        <h2 class = "profilevendor">
          <strong>License Number:</strong>
          <%= current_user.profile.license_number %>
        </h2>
        <td><%= link_to 'Show', current_user.profile %></td>
        <td><%= link_to 'Edit', edit_profile_path(current_user.profile) %></td>
        <% elsif %>
        <% current_user.profile %>
        <td><%= current_user.profile.name %></td>
        <td><%= image_tag current_user.profile.picture,
                          id: "HomeImage",
                          class: "profilepic" %></td>
        <td><%= current_user.profile.bio %></td>
        <td><%= link_to 'Show', current_user.profile %></td>
        <td><%= link_to 'Edit', edit_profile_path(current_user.profile) %></td>
        <% else %>
        <%= link_to 'New Profile', new_profile_path %>
        <% end %>


        </div>

      </tr>

  </tbody>
</table>
